const { expect } = require('chai');

describe('API > Google OAuth2 SignIn/Login', () => {
  describe('GET /forest/sessions-google', () => {
    it('should pass', () => {
      expect(1).equal(1);
    });
  });
});
